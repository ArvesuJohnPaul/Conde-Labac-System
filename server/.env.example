# Copy this file to ".env" and fill in your real Postgres password.
# .env is git-ignored so your password never gets committed.

PGHOST=localhost
PGPORT=5432
PGDATABASE=conde_labac
PGUSER=postgres
PGPASSWORD=changeme

# Port the API server listens on
PORT=3000
